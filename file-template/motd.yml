---
- name: configure SOE
  hosts: prod
  remote_user: devops
  become: true
  serial: 4
  vars:
    - system_owner: clyde@example.com
  tasks:
    - name: configure /etc/motd
      template:
        src: files/motd.j2
        dest: /etc/motd
        owner: root
        group: root
        mode: 0644
